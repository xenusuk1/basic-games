#lang "qb"

FUNCTION Dice (N AS INTEGER, A AS INTEGER) AS INTEGER
	Dice = INT((RND(1)*N) +1) + A
END FUNCTION

DIM R AS INTEGER, D AS INTEGER, A AS INTEGER

DO 
INPUT "D&A", D, A
R = Dice(D,A)
PRINT R
LOOP

